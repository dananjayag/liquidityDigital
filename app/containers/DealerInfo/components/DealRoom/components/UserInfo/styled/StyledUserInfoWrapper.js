import styled from 'styled-components';

const StyledUserInfoWrapper = styled.div`
    background: rgba(0, 0, 0, 0.45);
    position: fixed;
    width: 100vw;
    min-height: 100vh;
    border-radius: 5px 0px 0px 5px;
    overflow: auto;
    z-index: 100;
    top: 0;
    left: 0;

`;

export default StyledUserInfoWrapper;